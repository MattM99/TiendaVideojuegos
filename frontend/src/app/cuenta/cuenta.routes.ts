import { Routes } from "@angular/router";
import { CuentaListComponent } from "./cuenta-list/cuenta-list";

export const CUENTA_ROUTES: Routes = [
  {path: '', component: CuentaListComponent}  // /cuentas
];
